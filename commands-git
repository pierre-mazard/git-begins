# cloner un repository : 
	git clone <https://lien-github-du-repository>

# Les trois commandes PUSH de git : 
	git push origin <non-de-branche> : cette commande pousse les modifications locales apportées à la branche spécifiée vers la branche correspondante sur le dépôt distant nommé origin.
	git push --all : cette commande pousse toutes les branches locales vers le dépôt disant.
	git push --tags : cette commande pousse toues les balises locales vers le dépôt distant.
	
# Commande permettant de pull : 
	git pull <remote>
	
# Commande permettant de switcher d'une branche à une autre : 
	git branch : affiche les branches actuelles et met en évidance la branche chargée 
	git checkout <nom-de-branche> : permet de switcher d'une branche à une autre

